interface:
  display_name: "Croco Video Factory"
  short_description: "按同一 P1–P8 流程，用 Canvas 或原生后端制作知识视频"
  default_prompt: "使用 $croco-video-factory 完成这个视频；默认采用互动与本地 Canvas，按固定 P1–P8 流程执行，在 P2 锁定剧本，P6 后直接生成 P8 正式分镜，并用视频理解对照剧本与分镜规划做一致性闭环。"
