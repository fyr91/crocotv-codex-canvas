# 项目初始化规范

确认 `topic`、`targetAudience`、`contentDirection`、`mainCharacters`、`userDefinedNpcs`、可选 `visualStyle`。前三项必填。正式角色只保留用户指定项；缺角色时创建项目临时 NPC。未给风格时在策划阶段推断。项目目录为 `Projects/YYYYMMDDTHHmmss-中文主题`，内部使用中文目录、JSON 使用 camelCase。
